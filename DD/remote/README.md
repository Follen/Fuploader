# NetEase DD remote frontend snapshots

This directory contains publicly downloadable JavaScript bundles referenced by
the NetEase DD embedded WebView pages. The files are retained only to make the
endpoint and field analysis reproducible.

No cookies, access tokens, request captures, or account-specific responses may
be stored here.

